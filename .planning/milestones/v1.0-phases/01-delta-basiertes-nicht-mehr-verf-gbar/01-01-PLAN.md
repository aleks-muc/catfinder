---
phase: 01-delta-basiertes-nicht-mehr-verf-gbar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - catfinder.py
autonomous: false
requirements: [REPORT-01, REPORT-02, REPORT-03]
tags: [python, single-file, state-purge, delta-report, html-render]

must_haves:
  truths:
    - "Nach einem Lauf, in dem genau die Katzen X und Y vom Listing verschwunden sind, listet die 'Nicht mehr verfügbar'-Sektion im erzeugten Report genau X und Y (REPORT-01, Success Criterion 1)."
    - "Beim Folgelauf (nichts Neues verschwunden) sind X und Y weder in state/seen_cats.json noch in der 'Nicht mehr verfügbar'-Sektion (REPORT-02, Success Criterion 2)."
    - "Wenn beim aktuellen Lauf keine Katze verschwunden ist UND voriger State nicht-leer war, zeigt die Sektion den Hinweistext 'Seit dem letzten Lauf sind keine Katzen verschwunden. ✨' (REPORT-03, D-05, Success Criterion 3)."
    - "Bei leerem voriger State (Erstlauf, --reset, Cold-Start) wird die 'Nicht mehr verfügbar'-Sektion komplett ausgeblendet (D-07)."
    - "Ein bestehender State mit Zombie-Einträgen führt nach dem ersten Lauf zu einem sauberen State ohne Zombies (D-04, D-02, Success Criterion 4)."
    - "State bleibt valide JSON (atomic write via tempfile.mkstemp + os.replace bleibt der einzige Schreib-Pfad, D-02, Success Criterion 5)."
    - "Auch der 'no new cats'-Early-Return-Pfad purged den State und ruft save_state auf (sonst persistieren Zombies wenn keine neuen Katzen kommen)."
    - "State bleibt flat dict {cat_id → entry} — keine Schema-Erweiterung, kein _meta-Feld (D-01)."
    - "Empty-State nutzt das bestehende <div class='empty'>…</div>-Pattern und die bestehende .empty CSS-Klasse — kein neues CSS (D-06)."
    - "Kein Sicherheitsnetz gegen Mass-Purge — der bestehende RuntimeError bei 0 Katzen reicht (D-03)."
  artifacts:
    - path: "catfinder.py"
      provides: "Delta-Logik, Purge-Schritt, had_prior_state-Signal, Empty-State-Render, Early-Return-Fix"
      contains:
        - "had_prior_state = bool(state)"
        - "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨"
        - "had_prior_state: bool = False"
        - "Nicht mehr verfügbar (0)"
    - path: "state/seen_cats.json"
      provides: "Nach Lauf: nur Katzen aus dem aktuellen Listing (keine Zombies)"
      format: "valides JSON, dict[cat_id, entry]"
  key_links:
    - from: "main() — nach load_state()"
      to: "render_report(had_prior_state=...)"
      via: "neue lokale Variable had_prior_state = bool(state) sofort nach state = load_state()"
      pattern: "had_prior_state\\s*=\\s*bool\\(state\\)"
    - from: "main() — vor save_state()"
      to: "state dict"
      via: "in-place purge: alle cat_id aus state entfernen, die nicht in current_ids stehen"
      pattern: "for\\s+cid\\s+in\\s+list\\(state\\.keys\\(\\)\\)"
    - from: "main() — Early-Return-Pfad (no new cats)"
      to: "save_state(state) nach purge"
      via: "purge + save_state werden auch vor return 0 ausgeführt"
      pattern: "save_state\\(state\\)"
    - from: "render_report — sect_gone-Block"
      to: "Empty-State-Div"
      via: "elif had_prior_state: sect_gone = '<section>...<div class=\"empty\">...</div></section>'"
      pattern: "elif\\s+had_prior_state"
---

<objective>
Phase 1 schließt den Delta-Report ab: Die "Nicht mehr verfügbar"-Sektion im HTML-Report zeigt nur noch Katzen, die seit dem unmittelbar vorigen Lauf verschwunden sind. Der State `state/seen_cats.json` wird nach jedem Lauf in-place hart gepurged, sodass keine Zombie-Einträge mehr akkumulieren. Bei leerem Delta zeigt die Sektion einen klar lesbaren Hinweistext (außer bei Erstlauf / `--reset` — dort bleibt sie ausgeblendet).

Purpose: REPORT-01 (Delta-only), REPORT-02 (kein Zombie-State), REPORT-03 (Empty-State-Hinweis) gemeinsam abdecken — alle drei REQs hängen am gleichen Codepfad in `main()` und `render_report()` und werden in einem zusammenhängenden Edit-Block in `catfinder.py` geliefert.

Output: Modifizierte `catfinder.py` (Single-File-Konvention bleibt), kein neues Modul, keine neuen Dependencies, keine Änderung am CI-Workflow oder am Commit-Format.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-CONTEXT.md
@.planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-PATTERNS.md
@CLAUDE.md
@catfinder.py

<interfaces>
<!-- Aktuelle Signaturen und State-Shape — Executor nutzt diese direkt, keine Codebase-Exploration nötig. -->

State-Shape (catfinder.py:126):
```python
def load_state() -> dict[str, dict]:  # {cat_id: entry_dict}
def save_state(state: dict[str, dict]) -> None  # atomic write via tempfile.mkstemp + os.replace
```

Aktuelle render_report-Signatur (catfinder.py:548-555):
```python
def render_report(
    evaluated: list[tuple[Cat, CatRating]],
    total_listed: int,
    scope_note: str = "",
    listing_ages: dict[str, int | None] | None = None,
    still_known: list[tuple[Cat, CatRating]] | None = None,
    no_longer_listed: list[tuple[Cat, CatRating]] | None = None,
) -> str:
```

Erweiterte Signatur (Ziel — `had_prior_state` als zusätzlicher Parameter, default False):
```python
def render_report(
    evaluated: list[tuple[Cat, CatRating]],
    total_listed: int,
    scope_note: str = "",
    listing_ages: dict[str, int | None] | None = None,
    still_known: list[tuple[Cat, CatRating]] | None = None,
    no_longer_listed: list[tuple[Cat, CatRating]] | None = None,
    had_prior_state: bool = False,
) -> str:
```

Aktuelle sect_gone-Logik (catfinder.py:634-655) — Section wird heute NUR bei `no_longer_listed` truthy emittiert.

Atomic-write-Pattern, das UNVERÄNDERT bleibt (catfinder.py:136-147):
```python
def save_state(state: dict[str, dict]) -> None:
    STATE_DIR.mkdir(parents=True, exist_ok=True)
    payload = json.dumps(state, ensure_ascii=False, indent=2, sort_keys=True)
    fd, tmp_path = tempfile.mkstemp(prefix="seen_cats_", suffix=".json", dir=str(STATE_DIR))
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            f.write(payload)
        os.replace(tmp_path, STATE_FILE)
    except Exception:
        if os.path.exists(tmp_path):
            os.unlink(tmp_path)
        raise
```

Existierende `.empty`-CSS-Klasse, die wiederverwendet wird (catfinder.py:521):
```css
.empty { text-align: center; color: #666; padding: 4rem 1rem; }
```

Existierender Empty-State-Analog (catfinder.py:605):
```python
sect1_inner = '<div class="empty">Keine neuen Katzen seit dem letzten Lauf. 🎉</div>'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delta-Erfassung + Purge-Schritt + Early-Return-Fix in main()</name>
  <files>catfinder.py</files>
  <read_first>
    - catfinder.py (Zeilen 122-147 für State-I/O-Pattern, Zeilen 729-792 für main()-Eingangslogik und Early-Return-Pfad, Zeilen 842-859 für State-Update-Block am Lauf-Ende)
    - .planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-07)
    - .planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-PATTERNS.md (Sections 1, 2, 3 — State Purge Logic, Delta Computation, State-Update-Block + Early-Return-Branch)
    - CLAUDE.md (Logging-Konvention, German strings, Type-Hints, atomic-write-Pattern)
  </read_first>
  <action>
**Edit 1 — `had_prior_state` direkt nach `load_state()` erfassen (catfinder.py:733-734):**

Zeile 733-734 lautet aktuell:
```python
    state = load_state()
    known_ids = set(state.keys())
```

Ersetze durch (neue Zeile direkt nach load_state, vor known_ids):
```python
    state = load_state()
    had_prior_state = bool(state)  # D-07: Erstlauf vs. regulärer Lauf — voriger State nicht-leer?
    known_ids = set(state.keys())
```

**Edit 2 — Purge-Schritt + save_state im Early-Return-Pfad (catfinder.py:783-792):**

Aktueller Block (Zeilen 783-792):
```python
    if not to_evaluate:
        print("Keine neuen Katzen seit dem letzten Lauf.")
        la = {c.cat_id: _age_months_with_fallback(c.cat_id, c.age_hint) for c in still_known}
        la.update({c.cat_id: _age_months_with_fallback(c.cat_id, c.age_hint) for c, _ in no_longer_listed})
        html_text = render_report([], len(cats), listing_ages=la,
                                  still_known=_ratings_from_state(still_known),
                                  no_longer_listed=no_longer_listed)
        write_and_open_report(html_text, no_browser=args.no_browser)
        _write_github_output(0)
        return 0
```

Ersetze durch (neuer `had_prior_state`-Param an render_report, plus Purge + save_state vor return; Reihenfolge: render zuerst — gerendert wird gegen den Pre-Purge-State — dann purge, dann save_state, dann return):
```python
    if not to_evaluate:
        print("Keine neuen Katzen seit dem letzten Lauf.")
        la = {c.cat_id: _age_months_with_fallback(c.cat_id, c.age_hint) for c in still_known}
        la.update({c.cat_id: _age_months_with_fallback(c.cat_id, c.age_hint) for c, _ in no_longer_listed})
        html_text = render_report([], len(cats), listing_ages=la,
                                  still_known=_ratings_from_state(still_known),
                                  no_longer_listed=no_longer_listed,
                                  had_prior_state=had_prior_state)
        write_and_open_report(html_text, no_browser=args.no_browser)
        # Purge: nur Katzen aus dem aktuellen Listing bleiben im State (D-02).
        for cid in list(state.keys()):
            if cid not in current_ids:
                del state[cid]
        save_state(state)
        print(f"State aktualisiert: {len(state)} Katzen bekannt.")
        _write_github_output(0)
        return 0
```

**Edit 3 — `had_prior_state` an render_report im Haupt-Pfad durchreichen (catfinder.py:836-839):**

Aktueller Aufruf:
```python
    html_text = render_report(evaluated, total_listed=len(cats), scope_note=scope_note,
                              listing_ages=listing_ages,
                              still_known=_ratings_from_state(still_known),
                              no_longer_listed=no_longer_listed)
```

Ersetze durch:
```python
    html_text = render_report(evaluated, total_listed=len(cats), scope_note=scope_note,
                              listing_ages=listing_ages,
                              still_known=_ratings_from_state(still_known),
                              no_longer_listed=no_longer_listed,
                              had_prior_state=had_prior_state)
```

**Edit 4 — Purge-Schritt am Lauf-Ende, vor save_state (catfinder.py:842-857):**

Aktueller Block (Zeilen 842-857):
```python
    # State: alle aktuell gelisteten Katzen eintragen, Bewertungen speichern.
    now_iso = datetime.now().isoformat(timespec="seconds")
    for cat in cats:
        if cat.cat_id not in state:
            entry = asdict(cat)
            entry["first_seen"] = now_iso
            state[cat.cat_id] = entry
    for cat in to_evaluate:
        if cat.cat_id in ratings:
            state[cat.cat_id]["rating"] = ratings[cat.cat_id].rating
            state[cat.cat_id]["reason"] = ratings[cat.cat_id].reason
            state[cat.cat_id]["has_interested"] = cat.has_interested
            state[cat.cat_id]["companion_count"] = cat.companion_count
            state[cat.cat_id]["partner_name"] = cat.partner_name
    save_state(state)
    print(f"State aktualisiert: {len(state)} Katzen bekannt.")
```

Ersetze durch (Purge nach Insert/Update, vor save_state — Reihenfolge ist wichtig: erst aktuell gelistete eintragen/aktualisieren, dann verschwundene entfernen, dann atomic write):
```python
    # State: alle aktuell gelisteten Katzen eintragen, Bewertungen speichern.
    now_iso = datetime.now().isoformat(timespec="seconds")
    for cat in cats:
        if cat.cat_id not in state:
            entry = asdict(cat)
            entry["first_seen"] = now_iso
            state[cat.cat_id] = entry
    for cat in to_evaluate:
        if cat.cat_id in ratings:
            state[cat.cat_id]["rating"] = ratings[cat.cat_id].rating
            state[cat.cat_id]["reason"] = ratings[cat.cat_id].reason
            state[cat.cat_id]["has_interested"] = cat.has_interested
            state[cat.cat_id]["companion_count"] = cat.companion_count
            state[cat.cat_id]["partner_name"] = cat.partner_name
    # Purge: nur Katzen aus dem aktuellen Listing bleiben im State (D-02).
    for cid in list(state.keys()):
        if cid not in current_ids:
            del state[cid]
    save_state(state)
    print(f"State aktualisiert: {len(state)} Katzen bekannt.")
```

**WICHTIG für die Inline-Purge-Implementierung (statt separatem Helper):**
- Wir bauen den Purge bewusst zweimal inline (im Early-Return-Pfad und im Haupt-Pfad) statt eine `purge_disappeared(state, current_ids)`-Funktion in der `# --- State ---`-Section. Begründung: nur 3 Zeilen Code, klare lokale Lesbarkeit; Single-File-Konvention bleibt unangetastet; CONTEXT.md `<code_context>` erlaubt explizit beide Varianten und empfiehlt inline.
- `current_ids` ist im Haupt-Pfad bereits definiert in Zeile 759 (`current_ids = {c.cat_id for c in cats}`). Im Early-Return-Pfad ist `current_ids` ebenfalls verfügbar, weil der `current_ids`-Block in Zeile 759 VOR dem `if not to_evaluate`-Check kommt — also keine zusätzliche Definition nötig. Verifiziere durch Lesen von catfinder.py:759 vor und :783 nach Edit.
- `list(state.keys())` ist erforderlich: Iteration über `state.keys()` während der Mutation würde `RuntimeError: dictionary changed size during iteration` werfen. Snapshot via `list(...)` ist Standard-Idiom.
- KEINE neue Funktion in der `# --- State ---`-Section. KEINE Änderung an `save_state` oder `load_state`. Atomic-write-Pattern bleibt 1:1 erhalten.
- Bei `--reset` (catfinder.py:724-726) wird `STATE_FILE` gelöscht VOR `load_state()` — dadurch ist `state == {}` und `had_prior_state == False`. Kein Sondercode nötig.
  </action>
  <verify>
    <automated>cd /Users/aleksandarotasevic/Coding/Catfinder && python -c "import ast; ast.parse(open('catfinder.py').read())" && grep -c "had_prior_state = bool(state)" catfinder.py | grep -q "^1$" && grep -cE "for cid in list\(state\.keys\(\)\):" catfinder.py | grep -q "^2$" && grep -cE "if cid not in current_ids:" catfinder.py | grep -q "^2$" && grep -cE "had_prior_state=had_prior_state" catfinder.py | grep -q "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('catfinder.py').read())"` exits 0 (Syntax bleibt valide).
    - `grep -c "had_prior_state = bool(state)" catfinder.py` returns exactly `1` (Variable wird genau einmal gesetzt, direkt nach load_state).
    - `grep -cE "for cid in list\(state\.keys\(\)\):" catfinder.py` returns exactly `2` (Purge erscheint in Early-Return UND Haupt-Pfad).
    - `grep -cE "if cid not in current_ids:" catfinder.py` returns exactly `2` (zugehörige Bedingung ebenfalls 2x).
    - `grep -cE "had_prior_state=had_prior_state" catfinder.py` returns exactly `2` (an render_report im Early-Return UND im Haupt-Pfad durchgereicht).
    - `grep -F "# Purge: nur Katzen aus dem aktuellen Listing bleiben im State (D-02)." catfinder.py` exits 0 in beiden Stellen — `grep -cF "# Purge: nur Katzen aus dem aktuellen Listing bleiben im State (D-02)." catfinder.py` returns `2`.
    - `grep -cF "save_state(state)" catfinder.py` returns at least `2` (Early-Return UND Haupt-Pfad rufen save_state).
    - `grep -cF "State aktualisiert:" catfinder.py` returns at least `2` (Log-Line in Early-Return UND Haupt-Pfad).
    - `save_state`-Definition (catfinder.py:136-147) bleibt textuell unverändert: `grep -cF "fd, tmp_path = tempfile.mkstemp(prefix=\"seen_cats_\", suffix=\".json\", dir=str(STATE_DIR))" catfinder.py` returns exactly `1`.
    - Keine neue Funktion `purge_disappeared` o.ä. wurde hinzugefügt: `grep -cE "^def purge" catfinder.py` returns `0`.
  </acceptance_criteria>
  <done>
    `main()` erfasst `had_prior_state` direkt nach `load_state()`, reicht es an `render_report` in beiden Aufrufen durch (Early-Return + Haupt-Pfad), purged den State in beiden Pfaden vor `save_state(state)`, und der Early-Return-Pfad ruft `save_state(state)` jetzt auf (vorher nicht). Atomic-write-Pattern in `save_state` bleibt unverändert.
  </done>
</task>

<task type="auto">
  <name>Task 2: render_report-Signatur erweitern + Empty-State-Branch im sect_gone-Block</name>
  <files>catfinder.py</files>
  <read_first>
    - catfinder.py (Zeilen 498-545 für HTML_TEMPLATE inkl. .empty CSS-Klasse, Zeilen 548-687 für komplette render_report — insbesondere :603-605 als sibling Empty-State-Analog und :634-655 als sect_gone-Block)
    - .planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-CONTEXT.md (D-05, D-06, D-07)
    - .planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-PATTERNS.md (Section 4 — Empty-State Section Rendering)
    - CLAUDE.md (German strings, Type-Hints inkl. PEP 604, HTML-f-string-Pattern)
  </read_first>
  <action>
**Edit 1 — Signatur von render_report erweitern (catfinder.py:548-555):**

Aktuelle Signatur:
```python
def render_report(
    evaluated: list[tuple[Cat, CatRating]],
    total_listed: int,
    scope_note: str = "",
    listing_ages: dict[str, int | None] | None = None,
    still_known: list[tuple[Cat, CatRating]] | None = None,
    no_longer_listed: list[tuple[Cat, CatRating]] | None = None,
) -> str:
```

Ersetze durch (zusätzlicher `had_prior_state: bool = False` als letzter Parameter, defaultet auf False — backward-compatible):
```python
def render_report(
    evaluated: list[tuple[Cat, CatRating]],
    total_listed: int,
    scope_note: str = "",
    listing_ages: dict[str, int | None] | None = None,
    still_known: list[tuple[Cat, CatRating]] | None = None,
    no_longer_listed: list[tuple[Cat, CatRating]] | None = None,
    had_prior_state: bool = False,
) -> str:
```

**Edit 2 — sect_gone-Block: drei-Wege-Verzweigung (catfinder.py:634-655):**

Aktueller Block (Zeilen 634-655):
```python
    # Sektion 2 — nicht mehr verfügbare Katzen
    sect_gone = ""
    if no_longer_listed:
        cards = []
        for cat, rating in sorted(no_longer_listed, key=_card_sort_key):
            meta = RATING_META[rating.rating]
            age_months = get_age(cat.cat_id, cat.age_hint)
            age_data = str(age_months) if age_months is not None else "unknown"
            cards.append(f"""
    <div class="card" style="--accent: {meta['color']}; opacity: .6;" data-age-months="{age_data}" data-rating="{rating.rating}" data-companions="{_pair_attr(cat)}">
      {_img(cat)}
      <div class="body">
        <h2>{html.escape(cat.name)} <span style="color:#999;font-weight:400;font-size:.85rem;">#{html.escape(cat.cat_id)}</span></h2>
        <div class="meta">{_meta_line(cat, age_months)}</div>
        {_partner_line(cat)}
        {_interested_badge(cat)}
        <div class="rating">{meta['emoji']} {meta['label']}</div>
        <div class="reason">{html.escape(rating.reason)}</div>
        <a class="btn" href="{html.escape(cat.profile_url)}" target="_blank" rel="noopener" style="background:#9e9e9e;">Steckbrief öffnen →</a>
      </div>
    </div>""")
        sect_gone = f'<section><h2 class="group">🚫 Nicht mehr verfügbar ({len(no_longer_listed)})</h2><div class="grid">{"".join(cards)}</div></section>'
```

Ersetze durch (existierender `if no_longer_listed`-Branch unverändert; neue `elif had_prior_state`-Branch fügt Empty-State-Hint ein; sonst bleibt `sect_gone = ""`):
```python
    # Sektion 2 — nicht mehr verfügbare Katzen
    sect_gone = ""
    if no_longer_listed:
        cards = []
        for cat, rating in sorted(no_longer_listed, key=_card_sort_key):
            meta = RATING_META[rating.rating]
            age_months = get_age(cat.cat_id, cat.age_hint)
            age_data = str(age_months) if age_months is not None else "unknown"
            cards.append(f"""
    <div class="card" style="--accent: {meta['color']}; opacity: .6;" data-age-months="{age_data}" data-rating="{rating.rating}" data-companions="{_pair_attr(cat)}">
      {_img(cat)}
      <div class="body">
        <h2>{html.escape(cat.name)} <span style="color:#999;font-weight:400;font-size:.85rem;">#{html.escape(cat.cat_id)}</span></h2>
        <div class="meta">{_meta_line(cat, age_months)}</div>
        {_partner_line(cat)}
        {_interested_badge(cat)}
        <div class="rating">{meta['emoji']} {meta['label']}</div>
        <div class="reason">{html.escape(rating.reason)}</div>
        <a class="btn" href="{html.escape(cat.profile_url)}" target="_blank" rel="noopener" style="background:#9e9e9e;">Steckbrief öffnen →</a>
      </div>
    </div>""")
        sect_gone = f'<section><h2 class="group">🚫 Nicht mehr verfügbar ({len(no_longer_listed)})</h2><div class="grid">{"".join(cards)}</div></section>'
    elif had_prior_state:
        # D-05/D-06/D-07: voriger State nicht-leer, aber nichts verschwunden — Empty-State-Hint mit bestehendem .empty-Pattern.
        sect_gone = (
            '<section><h2 class="group">🚫 Nicht mehr verfügbar (0)</h2>'
            '<div class="empty">Seit dem letzten Lauf sind keine Katzen verschwunden. ✨</div>'
            '</section>'
        )
    # else: had_prior_state == False (Erstlauf / --reset / Cold-Start) — sect_gone bleibt "" (D-07: Sektion komplett ausblenden).
```

**Edit 3 — `two_sections`-Variable beobachten (catfinder.py:601):**

Bestehende Zeile (catfinder.py:601):
```python
    two_sections = bool(still_known or no_longer_listed)
```

Diese Variable steuert nur das Wrapping von `sect1` (catfinder.py:629-632, "Neu"-Sektion bekommt eigenen Header bei mehreren Sektionen). Sie bleibt UNVERÄNDERT — der neue Empty-State-Hint im `sect_gone` ändert nichts daran, ob "Neu"-Sektion einen Header braucht. Konkret: Bei Erstlauf gibt es keine `still_known` und keine `no_longer_listed` → `two_sections == False` → `sect1` ohne Header → korrekt. Bei regulärem Lauf mit nichts verschwunden gibt es immerhin `still_known` → `two_sections == True` → `sect1` mit Header → korrekt.

Daher: KEINE Änderung an Zeile 601 nötig. Falls der Executor unsicher ist, verifiziere durch Lesen von catfinder.py:601 + :629-632.

**Style-Guardrails:**
- Tripe-quoted f-string-style aus `sect1_inner` (catfinder.py:605) wurde nicht kopiert — der Empty-State hier nutzt einen mehrzeiligen Plain-String mit Klammer-Concatenation (so wie in PATTERNS.md Section 4 Analog B). Beide Varianten sind im Code-Style präsent; die Klammer-Variante hält die Zeilen unter 100 Chars und ist lesbarer.
- Emoji `✨` (Sparkles) am Ende ist exakt — kein Tipp-Trick wie `:sparkles:`. UTF-8-Source ist Standard im Projekt.
- Section-Title `🚫 Nicht mehr verfügbar (0)` — `(0)`-Suffix konsistent mit `({len(no_longer_listed)})` im populated Branch und `({len(evaluated_sorted)})` in `sect1` Header (catfinder.py:630).
- KEIN neues CSS, KEINE neue HTML-Klasse. `.empty` ist bereits in HTML_TEMPLATE definiert (catfinder.py:521).
  </action>
  <verify>
    <automated>cd /Users/aleksandarotasevic/Coding/Catfinder && python -c "import ast; ast.parse(open('catfinder.py').read())" && grep -F "had_prior_state: bool = False," catfinder.py && grep -F "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨" catfinder.py && grep -F "Nicht mehr verfügbar (0)" catfinder.py && grep -cE "^    elif had_prior_state:$" catfinder.py | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('catfinder.py').read())"` exits 0 (Syntax valide).
    - `grep -F "had_prior_state: bool = False," catfinder.py` exits 0 (neuer Parameter mit korrektem Default in Signatur).
    - `grep -F "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨" catfinder.py` exits 0 (exakter Wortlaut aus D-05, mit Sparkles-Emoji).
    - `grep -F "Nicht mehr verfügbar (0)" catfinder.py` exits 0 (Section-Header für Empty-State-Branch mit Count-Suffix `(0)`).
    - `grep -cE "^    elif had_prior_state:$" catfinder.py` returns exactly `1` (genau ein elif-Branch im sect_gone-Block, korrekt eingerückt).
    - `grep -cF '<div class="empty">' catfinder.py` returns at least `2` (existierender Empty-State im sect1_inner UND neuer im sect_gone — `.empty`-Klasse wird wiederverwendet).
    - Existierende `.empty`-CSS-Klasse bleibt unverändert: `grep -F ".empty { text-align: center; color: #666; padding: 4rem 1rem; }" catfinder.py` exits 0.
    - Bestehender `if no_longer_listed:`-Branch im sect_gone-Block bleibt funktional unverändert (Card-Render mit `opacity: .6` und `style="background:#9e9e9e;"`): `grep -F "opacity: .6;" catfinder.py` exits 0 UND `grep -F 'style="background:#9e9e9e;"' catfinder.py` exits 0.
    - Keine neue CSS-Regel oder HTML-Klasse hinzugefügt: `grep -cE "^\\.empty " catfinder.py` returns `1` (nur die existierende Definition); `grep -cF 'class="empty"' catfinder.py` returns at least `2` (Wiederverwendung, kein neuer Class-Name).
    - Existierende `render_report`-Aufruf-Sites kompatibel: `python -c "from catfinder import render_report; import inspect; assert inspect.signature(render_report).parameters['had_prior_state'].default is False"` exits 0.
  </acceptance_criteria>
  <done>
    `render_report`-Signatur akzeptiert `had_prior_state: bool = False` als zusätzlichen, backward-kompatiblen Parameter. Der `sect_gone`-Block hat drei Branches: (a) populated (unverändert), (b) `elif had_prior_state` mit dem exakten Wortlaut aus D-05 in einem `<div class="empty">`, (c) implizit `else` mit `sect_gone = ""` (Sektion ausgeblendet). Keine neuen CSS-Klassen, kein Test-Stub. Modul lädt sich syntaktisch sauber.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — Drei seeded Fixtures gegen reports/report.html validieren</name>
  <files>state/seen_cats.json (gesichert + restauriert), reports/report.html (generiert)</files>
  <read_first>
    - .planning/ROADMAP.md (5 Erfolgskriterien Phase 1)
    - .planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-CONTEXT.md (D-05, D-07)
    - catfinder.py (zur Inspektion bei Bedarf)
  </read_first>
  <what-built>
    Inline-Purge-Schritt in `main()` (Early-Return + Haupt-Pfad), `had_prior_state`-Signal von `main()` an `render_report()`, Empty-State-Branch im `sect_gone`-Block mit dem festgelegten deutschen Hinweistext, `save_state` wird auch im "no new cats"-Pfad aufgerufen. Kein neues Modul, keine neue Dependency, keine Workflow-Änderung.
  </what-built>
  <how-to-verify>
**Vorbereitung — sichern:**
1. Falls `state/seen_cats.json` existiert: `cp state/seen_cats.json state/seen_cats.json.bak` (Backup).
2. `export ANTHROPIC_API_KEY=...` (echter API-Key — Live-Listing wird gescraped, aber das ist OK; alternativ kann der Tester einen Lauf vorher machen, um einen "echten" State als Ausgangsbasis zu haben).

**Fixture A — "X und Y verschwinden" (Success Criterion 1, REPORT-01):**

1. Nimm den aktuell gespeicherten `state/seen_cats.json` (oder mache vorher einen sauberen Lauf, um einen aktuellen State zu erzeugen) als Ausgangspunkt.
2. Editiere `state/seen_cats.json` von Hand: füge zwei "Phantom"-Einträge mit synthetischen `cat_id`s hinzu, die garantiert NICHT im aktuellen Listing stehen — z.B. `"99999991"` und `"99999992"` mit minimalen Pflichtfeldern (`name`, `profile_url`, `rating: "unbekannt"`, `reason: ""`, `first_seen` setzen). Beispiel-Snippet zum Hinzufügen:
   ```json
   "99999991": {"cat_id": "99999991", "name": "TestKatze X", "profile_url": "https://example.invalid/x", "image_url": "", "breed": "", "sex": "", "age_hint": "", "has_interested": false, "companion_count": 0, "partner_name": "", "first_seen": "2026-05-05T00:00:00", "rating": "unbekannt", "reason": ""}
   ```
3. Führe aus: `python catfinder.py --no-browser`
4. Verifiziere im erzeugten `reports/report.html`:
   - `grep -F "TestKatze X" reports/report.html` UND `grep -F "TestKatze Y" reports/report.html` exits 0 (beide in der Sektion).
   - Section-Header `🚫 Nicht mehr verfügbar (` taucht auf — Count enthält 2 (oder `(2)` oder mehr falls echte Katzen ebenfalls verschwanden während des Laufs).
   - **Erwartet:** Genau die seeded Phantom-Einträge erscheinen in der "Nicht mehr verfügbar"-Sektion (plus etwaige real verschwundene Katzen, was OK ist — wir prüfen Delta-Korrektheit, nicht exakte Anzahl).
5. Verifiziere im aktualisierten `state/seen_cats.json`:
   - `grep -F "99999991" state/seen_cats.json` exits 1 (nicht-null exit — Phantom wurde gepurged).
   - `grep -F "99999992" state/seen_cats.json` exits 1 (gepurged).
   - `python -m json.tool state/seen_cats.json > /dev/null` exits 0 (valide JSON, Success Criterion 5).

**Fixture B — "Folgelauf, nichts verschwunden, voriger State nicht-leer" (Success Criterion 2 + 3, REPORT-02 + REPORT-03):**

1. Direkt nach Fixture A (State enthält nun keine Phantom-Einträge mehr, nur reale Katzen aus dem aktuellen Listing).
2. Führe nochmal aus: `python catfinder.py --no-browser`
3. Verifiziere im erzeugten `reports/report.html`:
   - `grep -F "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨" reports/report.html` exits 0 (Empty-State-Hint sichtbar — REPORT-03 erfüllt).
   - `grep -F "Nicht mehr verfügbar (0)" reports/report.html` exits 0 (Section-Header mit Count `(0)`).
   - `grep -F "TestKatze X" reports/report.html` exits 1 (TestKatze X taucht NICHT mehr im Report auf — REPORT-02 erfüllt).
4. Verifiziere im State:
   - `grep -F "99999991" state/seen_cats.json` exits 1 (bleibt entfernt).

**Fixture C — "Erstlauf / leerer State" (Success Criterion 3 Negativ-Fall, D-07):**

1. `python catfinder.py --reset --no-browser` (löscht State, startet frisch).
2. Verifiziere im erzeugten `reports/report.html`:
   - `grep -F "Nicht mehr verfügbar" reports/report.html` exits 1 (Sektion ist KOMPLETT ausgeblendet — Erstlauf-Verhalten gemäß D-07).
   - `grep -F "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨" reports/report.html` exits 1 (Hinweistext erscheint NICHT bei Erstlauf).
   - `grep -F "Erstlauf" reports/report.html` exits 0 (scope_note enthält "Erstlauf" — Sanity-Check).
3. Verifiziere im State:
   - `python -m json.tool state/seen_cats.json > /dev/null` exits 0 (valide JSON).
   - `grep -cF '"cat_id":' state/seen_cats.json` returns einen Wert > 0 (State wurde nach Lauf wieder gefüllt).

**Sanity — CI-Commit-Stabilität (Success Criterion 5):**
1. `git status` zeigt nur `state/seen_cats.json`, `reports/report.html` und `catfinder.py` als modifiziert — KEINE neuen Dateien (kein neues Modul, keine neue Dependency, kein Workflow-Edit).
2. `cat .github/workflows/catfinder.yml | grep -F "chore: state & report aktualisiert"` exits 0 (Commit-Message in Workflow unverändert).

**Wiederherstellen:**
- Optional: `mv state/seen_cats.json.bak state/seen_cats.json` falls die UAT-Läufe den State zerstört haben sollen.
  </how-to-verify>
  <resume-signal>
Sage "approved" wenn alle drei Fixtures pass'en und das Backup wiederhergestellt ist (oder bewusst durch echten State ersetzt wurde). Bei Abweichungen: beschreibe konkret, welcher Fixture-Schritt welches Verhalten zeigt — der Planner / Executor passt dann zielgerichtet an.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local FS → state file (`state/seen_cats.json`) | Schreibvorgang muss atomar sein; partieller Schreibfehler darf den State nicht zerstören. |
| script → HTML report (`reports/report.html`, `docs/index.html` in CI) | Cat-Daten (Name, Reason, URL) sind 3rd-party Inhalte vom Tierschutzverein-Listing — müssen escaped sein, bevor sie ins HTML gerendert werden. |
| CI runner → repository (`contents: write`) | Workflow committet `state/seen_cats.json` und `docs/index.html`. Form (Pfade, Permissions, Commit-Message) muss unverändert bleiben. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | State-Datei `state/seen_cats.json` (Purge in main) | mitigate | Purge findet auf in-memory dict statt; einziger Disk-Write bleibt `save_state` mit existierendem `tempfile.mkstemp` + `os.replace`-Pattern (catfinder.py:136-147). Atomic Write garantiert: bei Crash zwischen Purge und Disk-Write bleibt der vorige State intakt. KEINE neue I/O-Pfade. Verifiziert durch Acceptance Criterion: `save_state`-Definition unverändert, `mkstemp + os.replace`-Pattern bleibt einzige Schreibpfad. |
| T-01-02 | Denial of Service | State wird komplett gepurged bei leerem Listing (z.B. Site temporär down) | accept | Bestehender Schutz: `scrape_listing` wirft `RuntimeError` bei 0 gefundenen Katzen (catfinder.py:213-217) — Run terminiert vor dem Purge-Schritt, State bleibt unverändert. Recovery via `--reset` falls jemals fehlschlägt. Privater Single-User-Use-Case — kein zusätzliches Safety-Net per D-03. |
| T-01-03 | Information Disclosure / Tampering | HTML-Injection durch Cat-Felder (name, reason) im Empty-State-Branch | mitigate | Der neue Empty-State-Branch enthält ausschließlich Static-String "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨" — keine User-/Listing-Daten interpoliert, daher keine Injection-Surface. Existierende populated-Branch nutzt bereits `html.escape(...)` für alle Cat-Felder (catfinder.py:646-651) — bleibt unverändert. |
| T-01-04 | Repudiation | CI-Commit-Stabilität (Permissions, Pfade, Message) | mitigate | Workflow `.github/workflows/catfinder.yml` wird in dieser Phase **nicht** angefasst. Acceptance Criterion: `git status` zeigt nur `catfinder.py`, `state/seen_cats.json`, `reports/report.html` als modifiziert — keine Workflow-/Permission-Drift. |
| T-01-05 | Tampering | State-Datei wird in `--reset`-Pfad nicht versehentlich beim Empty-State-Render geschrieben | mitigate | `--reset` löscht `STATE_FILE` (catfinder.py:724-726) VOR `load_state()`. Danach ist `state == {}`, `had_prior_state == False`, und beim Lauf-Ende wird `state` regulär neu aufgebaut + atomic geschrieben. Inline-Purge-Schritt arbeitet auf dem rebuildeten state — keine Datenkorruption. |
| T-01-06 | Tampering | "Dictionary changed size during iteration"-Crash beim Purge | mitigate | Purge-Pattern verwendet explizit `for cid in list(state.keys()):` — Snapshot der Keys VOR der Iteration verhindert RuntimeError. Verifiziert durch Acceptance Criterion in Task 1. |
</threat_model>

<verification>
**Multi-Source Coverage Audit:**

| Source | ID/Item | Plan Coverage |
|--------|---------|---------------|
| GOAL (ROADMAP Phase 1) | "Report zeigt nur Run-zu-Run-Delta verschwundener Katzen, State ohne Zombies" | Tasks 1+2+3 (must_haves Truths 1, 2, 5) |
| REQ | REPORT-01 (Delta-only Sektion) | Task 1 (Purge im Haupt-Pfad), Task 3 Fixture A |
| REQ | REPORT-02 (Zombies aus State entfernt) | Task 1 (Purge in Haupt-Pfad + Early-Return-Pfad), Task 3 Fixture B |
| REQ | REPORT-03 (Empty-State-Hinweistext) | Task 2 (sect_gone elif-Branch), Task 3 Fixture B |
| RESEARCH | (keine RESEARCH.md für diese Phase — nicht erforderlich) | n/a |
| CONTEXT D-01 | flat dict, kein _meta-Feld | Task 1 (Inline-Purge mutiert dict, kein Schema-Änderung) |
| CONTEXT D-02 | Hard-Purge im Save-Schritt, Delta vor Purge berechnet | Task 1 (Purge nach State-Update, vor save_state; `no_longer_listed` aus :759-778 wird VOR der Purge-Mutation berechnet, weil Render davor passiert) |
| CONTEXT D-03 | Kein Safety-Net gegen Mass-Purge | T-01-02 in Threat-Model (accept) — bestehender RuntimeError reicht |
| CONTEXT D-04 | Big-Bang-Migration | Implizit durch D-02-Purge — keine Migrationscode-Pfade |
| CONTEXT D-05 | Exakter Wortlaut "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨" | Task 2 Edit 2, Acceptance Criterion + Task 3 Fixture B |
| CONTEXT D-06 | Bestehendes `<div class="empty">…</div>`-Pattern, kein neues CSS | Task 2 (kein neues CSS, Acceptance Criterion prüft `.empty`-CSS-Definition unverändert) |
| CONTEXT D-07 | Sektion komplett ausblenden bei leerem voriger State | Task 1 (`had_prior_state = bool(state)`) + Task 2 (`else: sect_gone bleibt ""`) + Task 3 Fixture C |

**Decision-Coverage:** D-01..D-07 alle abgedeckt. Keine Deferred Ideas (Zeitfenster, _meta, Banner, Modularisierung) im Plan.

**Reachability-Check:**
- Truth 1 ("Delta zeigt X+Y") erreichbar via Task 1 (Purge ändert nichts an `no_longer_listed`-Berechnung in :759-778; State-Quelle bleibt Pre-Run-State).
- Truth 2 ("X+Y aus State entfernt") erreichbar via Task 1 Edit 4 (Purge vor save_state) + Task 1 Edit 2 (Purge in Early-Return-Pfad).
- Truth 3 ("Empty-Hint bei nicht-leerem voriger State") erreichbar via Task 1 Edit 1 (`had_prior_state`) + Task 2 Edit 2 (`elif had_prior_state` Branch).
- Truth 4 ("Sektion ausblenden bei Erstlauf") erreichbar via Task 2 Edit 2 (`else`-Pfad lässt `sect_gone = ""`).
- Truth 5 ("Big-Bang erstmaliger Lauf entfernt Zombies") erreichbar via Task 1 Edit 4 — gleicher Purge greift.
- Truth 6 ("State bleibt valides JSON, atomic write bleibt") erreichbar weil Task 1 `save_state` nicht anfasst — Acceptance Criterion verifiziert.
- Truth 7 ("Early-Return purged + saved") erreichbar via Task 1 Edit 2 explizit.
</verification>

<success_criteria>
- catfinder.py kompiliert (`python -c "import ast; ast.parse(open('catfinder.py').read())"` exits 0).
- `had_prior_state = bool(state)` einmalig direkt nach `load_state()` (Task 1 Acceptance).
- Inline-Purge in genau zwei Pfaden (Early-Return + Haupt-Pfad) — `for cid in list(state.keys()): if cid not in current_ids: del state[cid]` Idiom (Task 1 Acceptance).
- `had_prior_state=had_prior_state` an `render_report` in beiden Aufrufen durchgereicht (Task 1 Acceptance).
- `render_report`-Signatur akzeptiert `had_prior_state: bool = False` als zusätzlichen, defaulted Parameter (Task 2 Acceptance).
- Exakter Wortlaut "Seit dem letzten Lauf sind keine Katzen verschwunden. ✨" im sect_gone-Block (Task 2 Acceptance).
- Drei UAT-Fixtures pass'en (Task 3): seeded Phantom-IDs erscheinen + werden gepurged, Folgelauf zeigt Hint, `--reset` blendet Sektion aus.
- `save_state`-Definition unverändert; `tempfile.mkstemp` + `os.replace`-Pattern bleibt einziger Schreibpfad.
- Workflow `.github/workflows/catfinder.yml` unverändert (`git status`-Sanity).
- Keine neuen Files, keine neuen Runtime-Dependencies (Single-File-Konvention bleibt).
</success_criteria>

<output>
After completion, create `.planning/phases/01-delta-basiertes-nicht-mehr-verf-gbar/01-01-SUMMARY.md` containing:
- Verifiziertes Ergebnis pro Acceptance Criterion (Task 1, Task 2, Task 3).
- Snippets der finalen Stellen in catfinder.py (Purge-Block, Empty-State-Branch, Signatur-Erweiterung).
- Notiz zur Big-Bang-Erstlauf-Erwartung (D-04): die "Nicht mehr verfügbar"-Sektion kann beim ersten Deploy einmalig sehr lang sein — dies ist erwartet und kein Bug.
- Bestätigung, dass keine RESEARCH.md / kein Workflow-Edit / keine neue Dependency nötig war.
</output>
