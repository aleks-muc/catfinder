---
phase: quick-260815-j3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - catfinder.py
  - test_report_sections.py
autonomous: true
requirements: [QUICK-260815-j3a]
estimate:
  tokens: 40000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01: Eine aktuell gelistete Katze mit has_interested=True erscheint ausschliesslich in der neuen Sektion — weder in 'Neu seit letztem Lauf' noch in 'Weiterhin verfuegbar'. Herausgezogen wird aus BEIDEN Listen (evaluated und still_known)."
    - "D-02: no_longer_listed bleibt ungefiltert — verschwundene Katzen mit Interessenten bleiben in 'Nicht mehr verfuegbar'."
    - "D-03: Karten der neuen Sektion werden ueber _render_card OHNE dimmed=True gerendert (kein Opacity-Dimming, 'seit X Tagen gelistet' bleibt sichtbar)."
    - "D-04: Sektionsreihenfolge im body: Neu seit letztem Lauf -> Nicht mehr verfuegbar -> Interessenten vorhanden -> Weiterhin verfuegbar."
    - "Der Header-Wert '{new_count} neu bewertet' und der an _write_github_output uebergebene Wert bleiben die Gesamtzahl neu bewerteter Katzen — unveraendert gegenueber heute."
    - "Die Slider-Grenzen decken weiterhin ALLE angezeigten Katzen ab; die Filterleiste erscheint auch dann, wenn nur die neue Sektion Karten enthaelt."
    - "Bei 0 Katzen mit Interessenten wird die neue Sektion komplett weggelassen — kein Empty-State-Block."
    - "Der clientseitige Filter greift in der neuen Sektion, weil die Karten dieselben data-*-Attribute tragen."
  artifacts:
    - "catfinder.py — render_report um die Interessenten-Sektion erweitert"
    - "test_report_sections.py — assert-basierter Selbst-Check im Stil von test_pairs.py"
  key_links:
    - "_card_sort_key wird auch fuer die neue Sektion als sort-key verwendet (Paare bleiben benachbart)"
    - "_render_card(dimmed=False) -> identische data-age-months/data-rating/data-companions/data-health -> _build_filter_bar braucht keine Aenderung"
    - "all_ages (Slider), filter_bar-Bedingung und two_sections beruecksichtigen die neue Gruppe"
---

<objective>
Katzen mit festen Interessenten (`Cat.has_interested == True`) bekommen im HTML-Report eine eigene Sektion zwischen "Nicht mehr verfuegbar" und "Weiterhin verfuegbar".

Purpose: Katzen mit Interessenten sind faktisch vergeben, sollen aber sichtbar bleiben. Heute liegen sie unsortiert zwischen den echten Kandidaten und verwaessern die beiden Hauptlisten.
Output: Geaenderte `render_report` in `catfinder.py` + `test_report_sections.py` als Regressionsschutz.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@catfinder.py
@test_pairs.py
</context>

<key_facts>
Vom Planner bereits verifiziert — nicht erneut pruefen:

1. **`_build_filter_bar` braucht KEINE Aenderung.** Das JS selektiert global via `document.querySelectorAll('.card')` (catfinder.py:487) und kennt keine Sektionslogik. Sektionen mit 0 sichtbaren Treffern werden heute schon nicht ausgeblendet — das gilt fuer alle bestehenden Sektionen gleichermassen und bleibt so.
2. **Offline-Rendering funktioniert.** `from catfinder import render_report` importiert sauber ohne `ANTHROPIC_API_KEY` und ohne Netzwerk (nur `main()` prueft den Key). Interpreter: `.venv/bin/python` (3.9.6).
3. **"Benny" existiert nicht mehr im State** (Purge-Logik, catfinder.py:1072). Aktuell tragen 5 Eintraege `has_interested: true`: SASCHA (260188), SVEN (260234), KIM (260610), URSULA (260966), BRAUNIE (261479). SVEN und KIM sind ein Paerchen — guter Beleg dafuer, dass `_card_sort_key` auch in der neuen Sektion Partner benachbart haelt.
4. **`new_count` wird heute aus `len(evaluated_sorted)` gebildet** (catfinder.py:888). Sobald `evaluated_sorted` gefiltert wird, muss der Template-Wert auf die UNgefilterte Eingabeliste umgestellt werden, sonst aendert sich das CI-Verhalten. `_write_github_output(len(evaluated))` in `main()` liest die Original-Liste und bleibt unangetastet.
5. **Sektionstitel:** `Interessenten vorhanden (N)` — wortgleich zum Badge in `_status_line` (catfinder.py:789). Kein besserer Wortlaut noetig; Ueberschrift und Badge teilen sich damit dasselbe Vokabular.
</key_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Interessenten-Sektion in render_report</name>
  <files>catfinder.py</files>
  <action>
In `render_report` (catfinder.py:728-892) die Katzen mit Interessenten aus den beiden gelisteten Gruppen herausziehen und als eigene Sektion rendern.

Aenderungen, alle innerhalb von `render_report`:

1. Direkt nach `evaluated_sorted = sorted(evaluated, key=_card_sort_key)` (Zeile 740) die Partitionierung einfuegen: eine Liste `interested` aus allen Paaren von `evaluated_sorted` PLUS `still_known`, deren `cat.has_interested` wahr ist, sortiert mit `key=_card_sort_key` (D-01). Anschliessend `evaluated_sorted` und `still_known` jeweils auf die Paare ohne `has_interested` reduzieren. `no_longer_listed` NICHT anfassen (D-02). Darueber ein deutscher Einzeiler-Kommentar, der das Warum nennt (faktisch vergeben, sollen sichtbar bleiben) und festhaelt, dass verschwundene Katzen bewusst ausgenommen sind.

2. Slider-Grenzen (Zeilen 842-848): die neue Gruppe muss in `all_ages` eingehen, sonst faellt ihre Altersspanne aus den Slider-Grenzen. Eine weitere `all_ages += [...]`-Zeile fuer `interested` ergaenzen, analog zu den drei bestehenden.

3. `filter_bar`-Bedingung (Zeile 849): `interested` als dritte Alternative aufnehmen, damit die Filterleiste auch erscheint, wenn nach der Partitionierung nur noch die neue Sektion Karten hat.

4. `two_sections` (Zeile 851): `interested` ebenfalls aufnehmen, damit die Ueberschrift "Neu seit letztem Lauf (N)" nicht wegfaellt, wenn nur die neue Sektion daneben steht.

5. Neue Sektion nach dem `sect_gone`-Block und vor dem `sect2`-Block anlegen — Variable `sect_int`, Default `""`. Nur wenn `interested` nicht leer ist: Karten ueber `_render_card(cat, rating)` ohne `dimmed`-Argument erzeugen (D-03) und in das Markup der bestehenden Sektionen giessen — `section` > `h2.group` mit Titel und Anzahl in Klammern > `div.grid` mit den Karten. Titel: `Interessenten vorhanden`. Ist `interested` leer, bleibt `sect_int` leer — bewusst KEIN Empty-State-Zweig wie bei `sect_gone`, sondern das Verhalten von `sect2`.

6. `body=` im `HTML_TEMPLATE.format`-Aufruf (Zeile 891): `sect_int` zwischen `sect_gone` und `sect2` einreihen (D-04).

7. `new_count=` im selben `format`-Aufruf (Zeile 888): auf die Laenge der UNgefilterten Eingabeliste `evaluated` umstellen, damit Header-Zahl und CI-Output unveraendert die Gesamtzahl neu bewerteter Katzen bleiben. Die Zahl in der Ueberschrift "Neu seit letztem Lauf (N)" darf schrumpfen — sie kommt weiterhin aus `len(evaluated_sorted)`.

Nicht aendern: `_build_filter_bar`, `_render_card`, `_status_line`, `main()`, `_write_github_output`, das State-Format, das HTML/CSS im Template. Keine neuen Imports, keine neuen Dependencies. Der Badge "Interessenten vorhanden" bleibt auf den Karten der neuen Sektion stehen (Redundanz zur Ueberschrift ist akzeptiert — Unterdruecken haette ein zusaetzliches Flag auf `_render_card` gekostet). Code-Stil wie im Rest der Datei: snake_case, 4 Spaces, deutsche Kommentare.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, dataclasses, catfinder as cf
s = json.load(open('state/seen_cats.json'))
f = {x.name for x in dataclasses.fields(cf.Cat)}
pairs = [(cf.Cat(**{k: v for k, v in e.items() if k in f}), cf._rating_from_entry(e)) for e in s.values()]
h = cf.render_report([], len(pairs), still_known=pairs, had_prior_state=True,
                     first_seen_map={k: v.get('first_seen', '') for k, v in s.items()})
o = [h.index(t) for t in ('Neu seit letztem Lauf', 'Nicht mehr verf', 'Interessenten vorhanden (', 'Weiterhin verf')]
assert o == sorted(o), ('Sektionsreihenfolge falsch', o)
sect = h.split('Interessenten vorhanden (')[1].split('Weiterhin verf')[0]
rest = h.split('Weiterhin verf')[1]
hits = [cid for cid, e in s.items() if e.get('has_interested')]
assert hits, 'kein Testfall im State'
for cid in hits:
    assert cid in sect, (cid, 'fehlt in der neuen Sektion')
    assert cid not in rest, (cid, 'steht noch in Weiterhin verfuegbar')
assert 'class=\"card gone\"' not in sect, 'Karten der neuen Sektion sind gedimmt'
assert 'id=\"filterBar\"' in h
print('ok:', len(hits), 'Katzen in der neuen Sektion, Reihenfolge', o)
"</automated>
  </verify>
  <done>Der aus dem realen State gerenderte Report enthaelt die vier Sektionen in der Reihenfolge Neu / Nicht mehr verfuegbar / Interessenten vorhanden / Weiterhin verfuegbar; alle 5 Katzen mit `has_interested` stehen in der neuen Sektion und in keiner der beiden anderen gelisteten Sektionen; ihre Karten sind nicht gedimmt.</done>
</task>

<task type="auto">
  <name>Task 2: Selbst-Check test_report_sections.py</name>
  <files>test_report_sections.py</files>
  <action>
Assert-basierten Selbst-Check anlegen, exakt im Stil von `test_pairs.py`: Shebang, deutscher Modul-Docstring mit Anlass, Import aus `catfinder`, kleine `_cat`-Fabrik, `main()` mit asserts, Abschluss-`print`, `if __name__ == "__main__"`. Kein pytest, keine Fixtures, kein Netzwerk, kein API-Key.

Synthetische Faelle (jeweils `render_report(...)` aufrufen und im zurueckgegebenen HTML pruefen — die cat_id ist pro Karte eindeutig und taugt als Suchtoken):
1. Eine Katze mit Interessenten aus `evaluated` und eine aus `still_known` landen beide in der neuen Sektion; die Karten ohne Interessenten bleiben in ihren urspruenglichen Sektionen (D-01).
2. Eine Katze mit Interessenten in `no_longer_listed` bleibt in "Nicht mehr verfuegbar" und taucht NICHT in der neuen Sektion auf (D-02).
3. Sektionsreihenfolge im HTML: Neu vor Nicht-mehr-verfuegbar vor Interessenten vor Weiterhin-verfuegbar (D-04) — ueber die Zeichenpositionen der vier Ueberschriften.
4. Keine Katze mit Interessenten: die Ueberschrift der neuen Sektion kommt im HTML nicht vor (kein Empty-State).
5. Nur Katzen mit Interessenten (`evaluated` und `still_known` danach leer): die Filterleiste wird trotzdem gerendert und die Ueberschrift "Neu seit letztem Lauf" ist vorhanden.
6. `new_count`: bei N uebergebenen `evaluated`-Katzen, von denen welche mit Interessenten herausfallen, steht im Header weiterhin N als Zahl der neu bewerteten Katzen, waehrend die Zahl in der Neu-Ueberschrift kleiner ist.

Anschliessend ein Smoke-Durchlauf gegen den realen `state/seen_cats.json`: Datei laden, `Cat`-Objekte ueber die Dataclass-Felder rehydrieren, `_rating_from_entry` fuer die Bewertungen nutzen, alles als `still_known` an `render_report` geben und generisch pruefen, dass jeder Eintrag mit `has_interested` in der neuen Sektion und in keiner anderen gelisteten Sektion steht. Keine festen Namen oder Anzahlen hart verdrahten — der State aendert sich zweimal taeglich. Fehlt die Datei oder enthaelt sie keinen solchen Eintrag, den Smoke-Teil ueberspringen und das per print vermerken.

Der Docstring haelt fest, dass der Check offline laeuft und `python catfinder.py` bewusst nicht aufgerufen wird (Live-Scrape + API-Calls).
  </action>
  <verify>
    <automated>.venv/bin/python test_report_sections.py && .venv/bin/python test_pairs.py</automated>
  </verify>
  <done>Beide Selbst-Checks laufen ohne Netzwerk und ohne ANTHROPIC_API_KEY gruen durch; `test_pairs.py` ist unveraendert und weiterhin gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fremde Website -> catfinder.py | Katzendaten (Name, Rasse, Bild-URL, Steckbrieftext) stammen von tierschutzverein-muenchen.de und sind untrusted input |
| catfinder.py -> GitHub Pages | Der gerenderte Report ist oeffentlich erreichbar (Repo ist public, Free-Plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j3a-01 | Tampering | Neue Sektion in `render_report` | low | mitigate | Die Sektion rendert ausschliesslich ueber das bestehende `_render_card`, das alle Fremdfelder durch `html.escape` schickt. Neu hinzu kommen nur ein String-Literal als Ueberschrift und ein `len()`-Integer — kein neuer ungeprufter Pfad in die HTML-Ausgabe. |
| T-j3a-02 | Information Disclosure | Oeffentlicher Report auf GitHub Pages | low | accept | Die Aenderung sortiert bereits heute veroeffentlichte Daten um; es werden keine zusaetzlichen Felder gerendert und keine personenbezogenen Daten hinzugefuegt. |
| T-j3a-03 | Denial of Service | Partitionierung ueber die Katzenlisten | low | accept | Zwei zusaetzliche Listendurchlaeufe bei < 100 Katzen. Laut CLAUDE.md ist Performance fuer dieses Projekt nicht relevant. |
| T-j3a-SC | Tampering | Package-Installs | n/a | n/a | Keine `pip install`-Schritte in diesem Plan — keine neuen Runtime-Dependencies, damit kein Supply-Chain-Vektor. |
</threat_model>

<open_questions>
**Diskrepanz Header-Zahl vs. Sektions-Zahl — Entscheidung liegt beim Nutzer, NICHT vom Executor loesen.**

Nach dieser Aenderung kann der Header "3 neu bewertet" melden, waehrend die Ueberschrift darunter "Neu seit letztem Lauf (2)" zeigt — weil eine der drei neuen Katzen in die Interessenten-Sektion abgewandert ist. Das ist die direkte Folge der harten Vorgabe, `new_count` und den CI-Output unangetastet zu lassen, und ist so beabsichtigt umgesetzt.

Falls die Zahlen zusammenpassen sollen, gibt es zwei Wege — beide beruehren Gesperrtes und sind daher hier NICHT eingeplant:
- Header-Text um einen Zusatz erweitern (z.B. Hinweis, wie viele davon Interessenten haben) — beruehrt `HTML_TEMPLATE`, nicht aber `new_count` selbst.
- `new_count` auf die gefilterte Zahl umstellen — wuerde den ntfy-/CI-Output aendern und verstoesst gegen die CLAUDE.md-Constraint.

Nach dem ersten realen Lauf mit neuen Katzen entscheiden.
</open_questions>

<verification>
1. `.venv/bin/python test_report_sections.py` — gruen (synthetische Faelle + Smoke gegen den realen State).
2. `.venv/bin/python test_pairs.py` — weiterhin gruen, Datei unveraendert.
3. `git diff --stat` — beruehrt sind ausschliesslich `catfinder.py` und die neue `test_report_sections.py`. Insbesondere NICHT: `.github/workflows/catfinder.yml`, `state/seen_cats.json`, `docs/index.html`, `requirements.txt`.
4. `python catfinder.py` bzw. `--no-browser` wird NICHT ausgefuehrt (Live-Scrape + API-Calls + State-Ueberschreibung).
</verification>

<success_criteria>
- Katzen mit `has_interested` erscheinen genau einmal im Report, in der neuen Sektion (D-01).
- "Nicht mehr verfuegbar" ist unveraendert, inkl. Katzen mit Interessenten (D-02).
- Karten der neuen Sektion sind nicht gedimmt und zeigen "seit X Tagen gelistet" (D-03).
- Sektionsreihenfolge: Neu -> Nicht mehr verfuegbar -> Interessenten vorhanden -> Weiterhin verfuegbar (D-04).
- Header-`new_count` und CI-`new_count` unveraendert; nur die Zahl in der Neu-Ueberschrift schrumpft.
- Neue Sektion bei 0 Treffern komplett abwesend; Filterleiste erscheint, sobald irgendeine gelistete Karte existiert.
- Keine neuen Dependencies, kein State-Format-Wechsel, `_build_filter_bar` unveraendert, alle neuen Strings auf Deutsch.
</success_criteria>

<output>
Create `.planning/quick/260815-j3a-report-sektion-fuer-katzen-mit-festen-in/260815-j3a-SUMMARY.md` when done
</output>
