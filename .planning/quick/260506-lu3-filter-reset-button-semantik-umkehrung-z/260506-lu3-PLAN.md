---
phase: 260506-lu3-filter-reset-button-semantik-umkehrung-z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - catfinder.py
autonomous: false
requirements:
  - QUICK-LU3-01
must_haves:
  truths:
    - "Klick auf den Button (rechts in der Filter-Leiste) schaltet ALLE aktiven Filter ab und zeigt jede Katze."
    - "Sorgenkinder werden nach Klick eingeblendet (showSorg=true)."
    - "Der Geeignet-Toggle ist nach Klick AUS (showOnlyFit=false), Label '🟢 Nur geeignet'."
    - "Der Pärchen-Toggle ist nach Klick AUS (showOnlyPair=false), Label '🐱🐱 Nur Pärchen' (ohne '(aktiv)')."
    - "Slider zeigt die volle Datenbandbreite (LO..HI), nicht den DEFAULT-Bereich."
    - "Der sichtbare Button-Text lautet '🌐 Alle Katzen zeigen'."
    - "Der Reset-Handler löst weiterhin nur EINEN update()-Aufruf am Ende aus (D-07 Atomarität)."
  artifacts:
    - path: "catfinder.py"
      provides: "Inverted reset semantics in _build_filter_bar resetBtn handler + new button label + DEFAULT_LO/HI cleanup"
      contains: "🌐 Alle Katzen zeigen"
  key_links:
    - from: "resetBtn click-handler in _build_filter_bar"
      to: "update() call at end of handler"
      via: "single trailing update() invocation"
      pattern: "update\\(\\);\\s*\\}\\)\\);\\s*if\\(minR\\)minR.addEventListener"
---

<objective>
Filter-Reset-Button-Semantik umkehren: Statt "Filter auf Default zurücksetzen" soll der Button jetzt "Alle Filter ausschalten / alle Katzen zeigen" bedeuten. Aktuelle Defaults (showOnlyFit=true, showOnlyPair=true, Slider auf DEFAULT_LO..DEFAULT_HI) werden invertiert: showOnlyFit=false, showOnlyPair=false, showSorg=true, Slider auf volle Datenbandbreite (LO..HI). Button-Label, Toggle-Labels und Toggle-Klassen-States müssen mitziehen.

Purpose: Der User will einen Schnellweg, um die ganze Liste ohne Filter zu sehen — nicht zurück zum Default-Filter.
Output: Modifizierter `_build_filter_bar` in `catfinder.py` mit invertierter Reset-Logik und gelöschtem `DEFAULT_LO/DEFAULT_HI`-JS-Code (nun unbenutzt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Aktueller relevanter Code in catfinder.py (Zeilen 337-403). Executor MUSS diese Strukturen kennen. -->

Aktueller Button im HTML (Zeile 341):
```html
<button id="resetBtn" style="margin-left:auto;">↺ Filter zurücksetzen</button>
```

Aktuelle JS-Variablen-Deklaration (Zeile 349):
```javascript
var LO={age_min},HI={age_max},DEFAULT_LO={default_lo},DEFAULT_HI={default_hi},showSorg=false,showOnlyFit=true,showOnlyPair=true;
```

Aktueller resetBtn-Handler (Zeilen 392-403):
```javascript
resetBtn.addEventListener('click',function(){{
  showSorg=false;showOnlyFit=true;showOnlyPair=true;
  if(minR)minR.value=DEFAULT_LO;
  if(maxR)maxR.value=DEFAULT_HI;
  fitBtn.textContent='🟢 Nur geeignet';
  fitBtn.classList.add('active');
  pairBtn.textContent='🐱🐱 Nur Pärchen (aktiv)';
  pairBtn.classList.add('active');
  sorgBtn.textContent='🔴 Sorgenkinder einblenden';
  sorgBtn.classList.add('hidden');
  update();
}});
```

Bezugspunkte (NICHT ändern):
- Slider-input (Zeilen 319-320): `value="{default_lo}"` / `value="{default_hi}"` — Initial-Values bleiben default. Nur der Reset-Klick weicht jetzt davon ab.
- pairBtn-Click-Handler (Zeile 374-379): Toggle-Logik bleibt; das `'(aktiv)'`-Suffix bei aktivem Pair-Filter bleibt erhalten.
- fitBtn-Click-Handler (380-385) und sorgBtn-Click-Handler (386-391): unverändert.
- Python-Berechnung von `default_lo`/`default_hi` in Zeilen 306-307: bleibt (wird für Slider-Initial-Values noch gebraucht).
- CSS für `#resetBtn` (Zeilen 334-335): bleibt unverändert.
- Doubled `{{ }}` und `{{...}}` in der f-string CSS/JS: bleibt — alle einfachen `{` müssen weiterhin `{{` sein. Nur `{age_min}`, `{age_max}`, `{default_lo}`, `{default_hi}` sind aktive f-string-Substitutions.

Constraint-Reminder (aus CLAUDE.md):
- Single-File: alles in `catfinder.py`.
- Strings deutsch.
- Inline CSS/JS in Python f-string: einfache `{`/`}` MÜSSEN gedoppelt sein (`{{`/`}}`). Wir touchen genau eine Variable-Deklarations-Zeile + die HTML-Button-Zeile + den resetBtn-Handler — alle drei sind bereits korrekt gedoppelt; weiter mit den existierenden Doppel-Braces arbeiten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset-Semantik invertieren + DEFAULT_LO/HI-Cleanup</name>
  <read_first>catfinder.py (Zeilen 297-410, _build_filter_bar)</read_first>
  <files>catfinder.py</files>
  <action>
Vier Änderungen innerhalb `_build_filter_bar`. Alle Edits präzise per Edit-Tool, keine pauschalen Rewrites. Doubled `{{ }}` für CSS/JS-Klammern beibehalten.

**Änderung A — Button-Label (Zeile 341):**
Ersetze:
```
  <button id="resetBtn" style="margin-left:auto;">↺ Filter zurücksetzen</button>
```
durch:
```
  <button id="resetBtn" style="margin-left:auto;">🌐 Alle Katzen zeigen</button>
```

**Änderung B — JS-var-Deklaration (Zeile 349): DEFAULT_LO/DEFAULT_HI entfernen.**
Ersetze:
```
  var LO={age_min},HI={age_max},DEFAULT_LO={default_lo},DEFAULT_HI={default_hi},showSorg=false,showOnlyFit=true,showOnlyPair=true;
```
durch:
```
  var LO={age_min},HI={age_max},showSorg=false,showOnlyFit=true,showOnlyPair=true;
```
(Achtung: `LO`, `HI`, `showSorg`, `showOnlyFit`, `showOnlyPair` bleiben — nur `DEFAULT_LO={default_lo},DEFAULT_HI={default_hi},` wird entfernt. f-string-Substitutions `{age_min}` und `{age_max}` bleiben.)

**Änderung C — resetBtn-Handler invertieren (Zeilen 392-403):**
Ersetze den kompletten Block:
```
  resetBtn.addEventListener('click',function(){{
    showSorg=false;showOnlyFit=true;showOnlyPair=true;
    if(minR)minR.value=DEFAULT_LO;
    if(maxR)maxR.value=DEFAULT_HI;
    fitBtn.textContent='🟢 Nur geeignet';
    fitBtn.classList.add('active');
    pairBtn.textContent='🐱🐱 Nur Pärchen (aktiv)';
    pairBtn.classList.add('active');
    sorgBtn.textContent='🔴 Sorgenkinder einblenden';
    sorgBtn.classList.add('hidden');
    update();
  }});
```
durch:
```
  resetBtn.addEventListener('click',function(){{
    showSorg=true;showOnlyFit=false;showOnlyPair=false;
    if(minR)minR.value=LO;
    if(maxR)maxR.value=HI;
    fitBtn.textContent='🟢 Nur geeignet';
    fitBtn.classList.remove('active');
    pairBtn.textContent='🐱🐱 Nur Pärchen';
    pairBtn.classList.remove('active');
    sorgBtn.textContent='🔴 Sorgenkinder ausblenden';
    sorgBtn.classList.remove('hidden');
    update();
  }});
```

Begründungen für die einzelnen Werte:
- `showSorg=true` → Sorgenkinder werden eingeblendet → sorgBtn-Text ist konsistent dazu `🔴 Sorgenkinder ausblenden` (gleiche Logik wie der sorgBtn-Click-Handler bei Zeile 388).
- `showOnlyFit=false` → fitBtn nicht aktiv → `classList.remove('active')`. Label bleibt `'🟢 Nur geeignet'` (passt zur sorgBtn-Click-Logik bei Zeile 382: bei showOnlyFit=false zeigt fitBtn `'🟢 Alle Bewertungen'` — ABER: nach Reset zeigt der Button konsistent zur Default-Initialisierung `'🟢 Nur geeignet'`, was den User-Intent „klick mich, um nur geeignete zu sehen" kommuniziert. Per task_intent: Label bleibt `🟢 Nur geeignet`.).
- `showOnlyPair=false` → pairBtn nicht aktiv → `classList.remove('active')`, Label `'🐱🐱 Nur Pärchen'` (ohne `(aktiv)`-Suffix).
- `minR.value=LO; maxR.value=HI;` → Slider auf volle Datenbandbreite, nicht auf DEFAULT_LO/HI.
- Single `update()` am Ende → D-07 Atomarität gewahrt.

**Änderung D — KEINE.** Die Python-Zeilen 306-307 (default_lo/default_hi-Berechnung) und die Slider-Initial-Values in Zeilen 319-320 bleiben unverändert: sie steuern weiterhin den initialen Slider-Stand beim Report-Load. Nur der Reset-Klick weicht jetzt von diesem Default ab.

Nach den Edits führe `python catfinder.py --no-browser` aus (siehe verify), um zu prüfen, dass die f-string keinen SyntaxError wirft.
  </action>
  <verify>
    <automated>
# 1) Neues Button-Label vorhanden, altes weg
grep -q '🌐 Alle Katzen zeigen' catfinder.py && \
! grep -q 'Filter zurücksetzen' catfinder.py && \
! grep -q '↺ Filter' catfinder.py && \
# 2) DEFAULT_LO/DEFAULT_HI komplett entfernt (kein Vorkommen mehr in der Datei)
! grep -q 'DEFAULT_LO' catfinder.py && \
! grep -q 'DEFAULT_HI' catfinder.py && \
# 3) Reset-Handler hat invertierte State-Werte
grep -q 'showSorg=true;showOnlyFit=false;showOnlyPair=false;' catfinder.py && \
# 4) Slider-Reset auf LO/HI (nicht DEFAULT_*)
grep -q 'if(minR)minR.value=LO;' catfinder.py && \
grep -q 'if(maxR)maxR.value=HI;' catfinder.py && \
# 5) fitBtn + pairBtn werden im Reset-Handler deaktiviert (remove statt add)
grep -q "fitBtn.classList.remove('active');" catfinder.py && \
grep -q "pairBtn.classList.remove('active');" catfinder.py && \
# 6) sorgBtn wird im Reset-Handler eingeblendet (remove statt add)
grep -q "sorgBtn.classList.remove('hidden');" catfinder.py && \
# 7) Reset-Labels passen zum neuen State
grep -q '🔴 Sorgenkinder ausblenden' catfinder.py && \
grep -q "pairBtn.textContent='🐱🐱 Nur Pärchen';" catfinder.py && \
# 8) D-07 Atomarität: genau ein update() im resetBtn-Handler
[ "$(awk '/resetBtn.addEventListener/,/}\\)\\);/' catfinder.py | grep -c 'update();')" = "1" ] && \
# 9) Python-Syntax + f-string ok (Skript darf nicht crashen vor dem API-Key-Check)
python3 -c "import ast; ast.parse(open('catfinder.py').read())" && \
echo "ALL_GREP_GATES_PASS"
    </automated>
  </verify>
  <done>
- `catfinder.py` enthält das neue Button-Label `🌐 Alle Katzen zeigen`.
- Kein Vorkommen mehr von `DEFAULT_LO`, `DEFAULT_HI`, `Filter zurücksetzen` oder `↺ Filter`.
- Reset-Handler setzt `showSorg=true`, `showOnlyFit=false`, `showOnlyPair=false`, Slider auf `LO`/`HI`.
- fitBtn/pairBtn werden auf `inactive` (`classList.remove('active')`) zurückgesetzt; sorgBtn wird sichtbar (`classList.remove('hidden')`).
- pairBtn-Label nach Reset = `🐱🐱 Nur Pärchen` (ohne `(aktiv)`).
- sorgBtn-Label nach Reset = `🔴 Sorgenkinder ausblenden`.
- Genau ein `update()`-Call innerhalb des resetBtn-Handlers.
- `python3 -c "import ast; ast.parse(open('catfinder.py').read())"` exit 0.
- Alle grep-Gates aus `<automated>` bestehen (Pipeline endet mit `ALL_GREP_GATES_PASS`).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser-Smoke-Test der invertierten Reset-Semantik</name>
  <what-built>
Der Reset-Button im Filter-Bar des HTML-Reports wurde semantisch umgekehrt: Klick → alle Filter aus, alle Katzen sichtbar, Slider auf volle Bandbreite. Button-Label ist `🌐 Alle Katzen zeigen`.
  </what-built>
  <how-to-verify>
1. Aus dem Repo-Root: `python catfinder.py --no-browser` ausführen, danach `reports/report.html` im Browser öffnen (oder: `python catfinder.py` ohne `--no-browser` öffnet automatisch).
   - Falls kein neues Listing nötig ist, geht auch: bestehenden `reports/report.html` direkt im Browser öffnen, sofern er nach dieser Änderung neu generiert wurde.
2. Im geöffneten Report:
   a. Filter manipulieren: Slider verschieben, "🐱🐱 Nur Pärchen" aktiv lassen, "🟢 Nur geeignet" aktiv lassen, "🔴 Sorgenkinder einblenden" einmal klicken (oder nicht — beliebiger Filter-Zustand).
   b. Den Button rechts in der Filter-Leiste — er heißt `🌐 Alle Katzen zeigen` — anklicken.
3. Erwartetes Verhalten nach dem Klick:
   - Slider zeigt die volle Bandbreite (linker Daumen ganz links, rechter Daumen ganz rechts).
   - "🟢 Nur geeignet" ist NICHT aktiv (grauer Hintergrund, nicht grün); Label bleibt `🟢 Nur geeignet`.
   - "🐱🐱 Nur Pärchen" ist NICHT aktiv (grau, nicht blau); Label `🐱🐱 Nur Pärchen` (ohne `(aktiv)`).
   - "🔴 Sorgenkinder einblenden" → Button-Label ist nun `🔴 Sorgenkinder ausblenden` und der Button ist NICHT mehr im roten Hidden-Stil (er ist sichtbar/aktiv-Stil aus dem CSS).
   - Alle Karten der Liste sind sichtbar (auch Sorgenkinder, auch Singles, auch Katzen außerhalb des Default-Altersfilters).
   - Der `#visibleCount`-Zähler in der Header-Zeile entspricht der Gesamtzahl der Katzen.
4. Sanity: Keine JS-Fehler in der Browser-Konsole (DevTools → Console; insbesondere kein `ReferenceError: DEFAULT_LO is not defined`).
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn das Verhalten passt, sonst beschreibe konkret was abweicht (z. B. "Slider bleibt auf Default-Werten" oder "Pärchen-Toggle bleibt aktiv").</resume-signal>
</task>

</tasks>

<verification>
- Alle grep-Gates aus Task 1 `<automated>` passieren.
- AST-Parse von `catfinder.py` exit 0.
- Manueller Browser-Smoke-Test (Task 2) bestätigt invertierte Semantik ohne JS-Konsolen-Fehler.
- Bestehende Toggle-Click-Handler (fitBtn / pairBtn / sorgBtn) sind unverändert und funktionieren weiterhin (visuell prüfen: nach Reset einmal `🐱🐱 Nur Pärchen` klicken → wird wieder aktiv; einmal `🔴 Sorgenkinder ausblenden` klicken → wird hidden).
</verification>

<success_criteria>
- Filter-Reset-Button hat neue Semantik: zeigt alle Katzen, schaltet alle Filter aus.
- Button-Label `🌐 Alle Katzen zeigen` sichtbar im Report.
- Keine JS-Errors im Browser; kein verwaister `DEFAULT_LO`/`DEFAULT_HI`-Code in der f-string.
- D-07 Atomarität (single `update()` am Ende des Handlers) bleibt erhalten.
- Single-File-Constraint, deutsche Strings, doubled `{{ }}` in CSS/JS-Block alle weiterhin gewahrt.
- Bot-Commit-Pfade, E-Mail-Subject und Pages-URL bleiben unverändert (keine Workflow-Änderungen).
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/quick/260506-lu3-filter-reset-button-semantik-umkehrung-z/260506-lu3-SUMMARY.md` mit:
- Was geändert wurde (Diff-Zusammenfassung der vier Edits in `catfinder.py`).
- Welche grep-Gates bestanden haben.
- Resultat des Browser-Smoke-Tests.
- Bestätigung, dass D-01..D-04 und D-07 unangetastet sind und D-06 jetzt obsolet ist.
</output>
