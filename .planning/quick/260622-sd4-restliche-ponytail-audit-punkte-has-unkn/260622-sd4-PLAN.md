---
quick_id: 260622-sd4
description: Restliche ponytail-Audit-Punkte (has_unknown, _pair_attr, detect_interested, Rating-Whitelist)
date: 2026-06-22
status: ready
---

# Quick Task 260622-sd4 — Restliche ponytail-Audit-Punkte

Vier kleine Over-Engineering-Cleanups in `catfinder.py`. Alle verhaltensneutral,
HTML-Output bleibt byte-identisch. Keine neuen Dependencies.

## Task 1: `has_unknown`-Parameter aus `_build_filter_bar` entfernen (delete)
- Signatur `def _build_filter_bar(age_min, age_max, has_unknown)` → ohne `has_unknown`
  (Parameter wird im Body nie genutzt).
- Call-Site `_build_filter_bar(age_min, age_max, False)` → `_build_filter_bar(age_min, age_max)`.

## Task 2: `_pair_attr` inlinen (yagni)
- Closure `_pair_attr(cat)` (reiner Wrapper um `str(cat.companion_count)`) entfernen.
- Einzige Nutzung in `_render_card`: `data-companions="{_pair_attr(cat)}"` →
  `data-companions="{cat.companion_count}"` (f-string formatiert int identisch zu `str()`).

## Task 3: `detect_interested` inlinen (shrink)
- Funktion `detect_interested(text)` (Einzeiler-Wrapper) entfernen.
- Einzige Nutzung in `main`: `cat.has_interested = detect_interested(text)` →
  `cat.has_interested = bool(INTERESTED_PATTERN.search(text))`.

## Task 4: Rating-Whitelist dedupen (shrink)
- Modul-Helper `_safe_rating(value: str) -> str` einführen (im State-Abschnitt),
  der einen aus dem State gelesenen Wert auf eine gültige `Rating`-Kategorie klemmt
  (sonst `"unbekannt"`).
- Beide duplizierten Blöcke
  `r = entry.get("rating", "unbekannt"); if r not in (...): r = "unbekannt"`
  durch `r = _safe_rating(entry.get("rating", "unbekannt"))` ersetzen.

## Verify
- `python3 -c "import ast; ast.parse(open('catfinder.py').read())"` fehlerfrei.
- Fixture-Render-Diff (wie bei 260622-s6i, Timestamp herausgefiltert) gegen den
  Stand vor diesem Task **leer** (Tasks 1–2 berühren HTML-Pfad; 3–4 nicht).
- `grep` bestätigt: keine Vorkommen von `has_unknown`, `_pair_attr`, `detect_interested`
  mehr; genau eine `_safe_rating`-Definition + zwei Aufrufe.

## Done
- 4 Cleanups angewandt, ein atomarer Commit, Output unverändert.
