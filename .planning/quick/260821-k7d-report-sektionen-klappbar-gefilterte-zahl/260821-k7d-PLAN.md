---
phase: quick-260821-k7d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - catfinder.py
autonomous: true
requirements: [QUICK-260821-k7d]
estimate:
  tokens: 15000
  raw_tokens: 9000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "D-01: Alle vier Report-Sektionen sind zusammenklappbar; umgesetzt mit nativem <details>/<summary> statt eigenem JS-Toggle."
    - "D-02: Nur 'Neu seit letztem Lauf' ist per Default offen (open-Attribut); die drei uebrigen Sektionen starten zugeklappt."
    - "D-03: Der Sektions-Zaehler zeigt bei aktiver Filterung 'sichtbar von gesamt', ohne Filter nur die Gesamtzahl."
    - "D-04: Die Filter-Logik bleibt in _build_filter_bar (inline CSS/JS im Python-f-string) — kein Build-Step, keine neue Dependency."
    - "D-05: Bestehende Selbst-Checks (test_report_sections.py, test_pairs.py, test_interested_refresh.py) bleiben gruen."
---

# Quick Task: Report-Sektionen klappbar + gefilterte Anzahl

Zwei Aenderungen an der Ergebnisdarstellung:

1. Die vier Sektionen (Neu seit letztem Lauf / Nicht mehr verfuegbar / Interessenten
   vorhanden / Weiterhin verfuegbar) werden zu `<details class="sect">`. Nur die
   Neu-Sektion traegt `open`.
2. Der Zaehler in der Ueberschrift wird von `(N)` zu `(<span class="cnt">N</span>)`.
   Die bestehende `filter()`-Funktion im Filter-Bar-JS aktualisiert nach jedem Lauf
   pro Sektion den Zaehler auf `sichtbar von gesamt` (bzw. nur `gesamt`, wenn nichts
   herausgefiltert ist).

## Task 1: _section-Helper + details-Markup + JS-Zaehler

- `_section(title, total, inner, is_open)` neben `_card_sort_key` (Sektion HTML-Report).
- `render_report`: vier Sektions-Literale auf den Helper umstellen.
- `HTML_TEMPLATE`-CSS: `section h2.group` -> zusaetzlich `summary h2.group`, plus
  `details.sect`/`summary`-Regeln (Cursor, Marker-Position).
- `_build_filter_bar`: am Ende von `filter()` pro `details.sect` sichtbare Karten
  zaehlen und `.cnt` setzen.

**Verification:** `.venv/bin/python test_report_sections.py`, `test_pairs.py`,
`test_interested_refresh.py`; Report aus vorhandenem State rendern und Markup pruefen.
